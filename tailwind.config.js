/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // ← обязательно!
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
}